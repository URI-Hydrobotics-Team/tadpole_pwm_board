# tadpole_pwm_board
this is a pwm IO extender board for tadpole to allow it to control its 8 thrusters via i2c, there will also be a watchdog microcontroller that will disable PWM output if it does not receive an OK signal from the main computer at some interval. 
